fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
python-dotenv>=1.0
psycopg2-binary>=2.9
boto3>=1.34
pyjwt[crypto]>=2.9

# Shared with training/ (imported directly at runtime — see src/ml, src/features/feature_store.py).
# xgboost pinned <3.0: newer versions break shap's TreeExplainer (see training/requirements.txt).
# scikit-learn pinned <1.6: 1.6 replaced ClassifierMixin's `_estimator_type` class
# attribute with a new tag system; xgboost 2.1.x's Booster.save_model() still reads
# the old attribute directly, so mlflow.xgboost.log_model() raises
# "TypeError: _estimator_type undefined" against sklearn>=1.6.
# fairlearn is only used by training/src/fairness.py, but this image doubles as the
# ml-train.yml ECS task's image (see credit-service/training/README.md), so it's included here too.
mlflow>=2.15
pandas>=2.1
numpy>=1.26
scikit-learn>=1.4,<1.6
xgboost>=2.1,<3.0
shap>=0.45
fairlearn>=0.10
