pandas>=2.1
numpy>=1.26
scikit-learn>=1.4
# Pinned <3.0: xgboost 3.x serializes base_score in a format shap 0.49's
# TreeExplainer cannot parse (ValueError: could not convert string to float).
xgboost>=2.1,<3.0
shap>=0.45
fairlearn>=0.10
mlflow>=2.15
# mlflow's Postgres backend store + data_loading.py's S3 fallback + the
# training-runs/latest.json pointer file (train.py) all need these.
psycopg2-binary>=2.9
boto3>=1.34
